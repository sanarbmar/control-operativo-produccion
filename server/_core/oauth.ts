// El login por OAuth de Manus fue reemplazado por autenticación propia
// (usuario/contraseña) en server/routers.ts (auth.login / auth.register).
// Este archivo ya no se usa y se deja vacío intencionalmente.
export {};
